#include<iostream>
using namespace std;

typedef struct NODE{
int data;
struct NODE *next;
}Node;
Node *add_edge(int a,int b,Node *c){
    for
}
int main(){
    int n;
    cout<<"Enter the no. of vertices\n";
    cin>>n;
    Node* a[n];
    int b;
    for(int i=0;i<n;i++){
        a[i]=new Node;
        cout<<"Enter the vertex"<<endl;
        cin>>a[i]->data;
    }
    while
    return 0;
}